services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - ~/.ssh/id_ed25519:/root/.ssh/id_ed25519:ro
      - ~/.ssh/known_hosts:/root/.ssh/known_hosts:ro
    environment:
      - GH_TOKEN=${GH_TOKEN}
    stdin_open: true
    tty: true
